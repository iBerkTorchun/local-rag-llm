# Foundry Local

Foundry Local enables large language models to run locally on a user's device.

It provides a lightweight runtime and SDK.

Foundry Local provides a curated catalog of optimized models.

Applications can perform local model inference without sending application data to a cloud model. This makes Foundry Local suitable for an offline local Q&A assistant.

