# Retrieval-Augmented Generation

RAG stands for Retrieval-Augmented Generation.

It follows Retrieve → Augment → Generate. Relevant information is first retrieved from a document collection.

The retrieved information is added to the model input as context. The model generates its answer using that context.

RAG helps reduce hallucinations and produces answers grounded in the supplied documents.

