# SQLite

SQLite is lightweight, serverless, and self-contained.

A SQLite database can be stored as a single local file. It does not require a separate database server.

SQLite is suitable for local data storage in this project.

We use SQLite to persist document chunks and their embedding vectors.

