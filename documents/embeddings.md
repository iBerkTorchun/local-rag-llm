# Embeddings

Text embeddings are numerical vector representations of text meaning.

Semantically similar text should have similar vector representations.

Embeddings enable semantic similarity search.

The same embedding model must be used for stored document chunks and incoming queries. This project uses embeddings as the retrieval representation for RAG.

