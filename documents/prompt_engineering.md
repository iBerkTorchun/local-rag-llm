# Prompt Engineering

System messages and user messages have different roles.

The model should be instructed to answer using the retrieved context.

The model should not invent unsupported information.

When the supplied context is insufficient, the assistant should say that the information is not available. Prompt design affects how reliably the model follows retrieved information.

